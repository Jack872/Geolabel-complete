修复请在后边打✅
示例：
遥感影像不加载✅

服务管理页面删除服务，影像集中任然显示已发布且再次发布报错

审核bug，审核通过时删除多标无法实现

一个任务按影像集上传多张影像时，底图服务只展示一个影像名，开始标注时点击下一张影像提示“已是最后一个任务”，实际后面还有影像。我看上面已经有影像选择切换遥感影像了，是不是可以把下面的给删了

标注页面回滚bug，点击回滚后无反应，只有标注下一个样本时才会回滚

标注页面使用删除选中标注时，点击选中一个样本同时也变成绘制下一个样本