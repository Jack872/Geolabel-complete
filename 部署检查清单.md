# 🚀 部署检查清单

## 前端部署

### 1. 文件检查
- [ ] `Frontend(React)/portal/src/pages/modelManage/style.less` 已创建
- [ ] `Frontend(React)/portal/src/pages/personalTaskList/style.less` 已创建
- [ ] `Frontend(React)/portal/src/pages/modelManage/components/ModelDetailsDrawer.jsx` 已创建
- [ ] `Frontend(React)/portal/src/pages/modelManage/components/ModelDetailsDrawer.less` 已创建
- [ ] `Frontend(React)/portal/src/services/map/api.js` 已更新（添加reqGetModelTrainDetails）

### 2. 代码修改检查
- [ ] `modelManage/index.jsx` 已添加样式引用
- [ ] `modelManage/index.jsx` 已添加ModelDetailsDrawer组件
- [ ] `modelManage/index.jsx` 已添加"查看详情"按钮
- [ ] `personalTaskList/index.jsx` 已添加样式引用
- [ ] 所有文件通过语法检查（无错误）

### 3. 构建和测试
```bash
# 进入前端目录
cd Frontend(React)/portal

# 安装依赖（如果需要）
npm install

# 启动开发服务器
npm start

# 或构建生产版本
npm run build
```

### 4. 浏览器测试
- [ ] 模型管理页面样式正常显示
- [ ] 个人任务列表页面样式正常显示
- [ ] 选项卡切换功能正常
- [ ] 模型详情抽屉可以打开
- [ ] 响应式布局在移动端正常

---

## 后端部署

### 1. Python FastAPI

#### 文件检查
- [ ] `Backend(FastAPI)/get_model_details.py` 已创建
- [ ] `Backend(FastAPI)/main.py` 已添加新接口

#### 依赖检查
确保以下Python包已安装：
```bash
pip install fastapi
pip install uvicorn
pip install psycopg2-binary  # 或 psycopg2
pip install torch
pip install rasterio
pip install shapely
```

#### 启动服务
```bash
# 进入Python后端目录
cd Backend(FastAPI)

# 启动FastAPI服务
python -m uvicorn main:app --host 0.0.0.0 --port 5000 --reload
```

#### 验证服务
- [ ] 访问 http://localhost:5000/docs 查看API文档
- [ ] 确认 `/getModelTrainDetails` 接口已列出

### 2. Spring Boot

#### 文件检查
- [ ] `Backend(SpringBoot)/src/main/java/com/example/labelMark/controller/MarkController.java` 已更新

#### 编译检查
```bash
# 进入Spring Boot目录
cd Backend(SpringBoot)

# 清理并编译
mvn clean compile

# 或使用Maven wrapper
./mvnw clean compile
```

#### 启动服务
```bash
# 使用Maven启动
mvn spring-boot:run

# 或使用Maven wrapper
./mvnw spring-boot:run

# 或运行打包后的jar
java -jar target/labelMark-0.0.1-SNAPSHOT.jar
```

#### 验证服务
- [ ] 服务在端口1290正常启动
- [ ] 查看启动日志无错误
- [ ] 访问 http://localhost:1290 确认服务可用

---

## 数据库检查

### 1. 表结构验证
确认 `model` 表包含以下字段：
```sql
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'model';
```

必需字段：
- [ ] model_id (主键)
- [ ] model_name
- [ ] model_des
- [ ] task_type
- [ ] model_type
- [ ] input_num
- [ ] output_num
- [ ] mapping
- [ ] path
- [ ] user_id
- [ ] create_time
- [ ] status (可选，建议添加)

### 2. 测试数据
插入测试数据（可选）：
```sql
INSERT INTO model (
  model_name, model_des, task_type, model_type,
  input_num, output_num, mapping, path, user_id
) VALUES (
  '测试模型', '用于测试的模型', '目标检测', 'yolo',
  3, 5, '通道映射信息', '/path/to/model.pt', 10
);
```

---

## 集成测试

### 1. API测试

#### 测试1：获取模型列表
```bash
curl -X POST http://localhost:1290/wegismarkapi/mark/getModelList \
  -H "Content-Type: application/json" \
  -d '{"user_id": "10", "task_type": "目标检测"}'
```
预期结果：返回模型列表

#### 测试2：获取模型详情
```bash
curl -X POST http://localhost:1290/wegismarkapi/mark/getModelTrainDetails \
  -H "Content-Type: application/json" \
  -d '{"model_id": 1, "user_id": 10}'
```
预期结果：返回模型详细信息

### 2. 前端功能测试

#### 模型管理页面
1. [ ] 访问模型管理页面
2. [ ] 点击"启动模型训练"按钮
3. [ ] 在抽屉中选择任务
4. [ ] 配置训练参数
5. [ ] 点击"批量训练"按钮
6. [ ] 查看模型列表
7. [ ] 点击"查看详情"按钮
8. [ ] 验证模型详情正确显示

#### 个人任务列表页面
1. [ ] 访问个人任务列表页面
2. [ ] 切换"团队任务"和"非团队任务"选项卡
3. [ ] 查看任务列表
4. [ ] 点击"开始标注"按钮
5. [ ] 点击"提交任务"按钮

### 3. 样式测试

#### 桌面端 (1920x1080)
- [ ] 模型管理页面布局正常
- [ ] 个人任务列表页面布局正常
- [ ] 表格显示完整
- [ ] 按钮样式正确
- [ ] 渐变色显示正常

#### 平板端 (768x1024)
- [ ] 页面自适应调整
- [ ] 抽屉宽度适配
- [ ] 表格可横向滚动
- [ ] 按钮大小适中

#### 移动端 (375x667)
- [ ] 选项卡垂直排列
- [ ] 表格字体缩小
- [ ] 按钮垂直排列
- [ ] 抽屉全屏显示

---

## 性能检查

### 1. 前端性能
- [ ] 页面加载时间 < 3秒
- [ ] 组件渲染流畅
- [ ] 动画效果流畅（60fps）
- [ ] 无内存泄漏

### 2. 后端性能
- [ ] API响应时间 < 1秒
- [ ] 数据库查询优化
- [ ] 文件读取效率
- [ ] 并发请求处理

### 3. 资源使用
- [ ] CPU使用率正常
- [ ] 内存使用率正常
- [ ] 磁盘I/O正常
- [ ] 网络带宽充足

---

## 安全检查

### 1. 权限验证
- [ ] 用户只能查看自己的模型
- [ ] API接口有权限验证
- [ ] 敏感信息已加密
- [ ] SQL注入防护

### 2. 数据验证
- [ ] 输入参数验证
- [ ] 文件路径验证
- [ ] JSON格式验证
- [ ] 错误信息不泄露敏感数据

---

## 日志和监控

### 1. 日志配置
- [ ] Spring Boot日志级别设置
- [ ] Python日志配置
- [ ] 错误日志记录
- [ ] 访问日志记录

### 2. 监控指标
- [ ] API调用次数
- [ ] 响应时间
- [ ] 错误率
- [ ] 系统资源使用

---

## 文档检查

- [ ] API文档完整
- [ ] 代码注释清晰
- [ ] 部署文档准备
- [ ] 用户手册更新

---

## 回滚计划

### 备份
- [ ] 数据库备份
- [ ] 代码版本标记
- [ ] 配置文件备份

### 回滚步骤
1. 停止服务
2. 恢复代码到上一版本
3. 恢复数据库（如需要）
4. 重启服务
5. 验证功能

---

## 上线清单

### 上线前
- [ ] 所有测试通过
- [ ] 代码审查完成
- [ ] 文档更新完成
- [ ] 备份已完成
- [ ] 回滚计划准备

### 上线中
- [ ] 停止旧服务
- [ ] 部署新代码
- [ ] 启动新服务
- [ ] 验证服务状态

### 上线后
- [ ] 功能验证
- [ ] 性能监控
- [ ] 错误日志检查
- [ ] 用户反馈收集

---

## 常见问题

### Q1: 前端无法连接后端
**解决方案**：
1. 检查后端服务是否启动
2. 检查端口号配置
3. 检查防火墙设置
4. 检查CORS配置

### Q2: 模型详情显示为空
**解决方案**：
1. 检查模型文件路径
2. 检查训练日志文件
3. 检查数据库数据
4. 查看后端日志

### Q3: 样式显示异常
**解决方案**：
1. 清除浏览器缓存
2. 检查CSS文件是否加载
3. 检查浏览器兼容性
4. 检查less编译

---

## 联系方式

如有问题，请联系：
- 开发团队
- 技术支持

---

**检查日期**：________
**检查人员**：________
**审核人员**：________
