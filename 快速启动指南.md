# 🚀 快速启动指南

## 概述

本指南帮助你快速启动优化后的模型管理和训练系统。

---

## ⚡ 快速启动（3步）

### 1️⃣ 启动Python后端
```bash
cd Backend(FastAPI)
python -m uvicorn main:app --host 0.0.0.0 --port 5000 --reload
```

✅ 验证：访问 http://localhost:5000/docs

### 2️⃣ 启动Spring Boot后端
```bash
cd Backend(SpringBoot)
./mvnw spring-boot:run
```

✅ 验证：访问 http://localhost:1290

### 3️⃣ 启动前端
```bash
cd Frontend(React)/portal
npm start
```

✅ 验证：访问 http://localhost:8000

---

## 📋 详细启动步骤

### Python FastAPI后端

#### 环境准备
```bash
# 安装Python依赖
pip install fastapi uvicorn psycopg2-binary torch rasterio shapely
```

#### 启动服务
```bash
cd Backend(FastAPI)

# 开发模式（自动重载）
python -m uvicorn main:app --host 0.0.0.0 --port 5000 --reload

# 生产模式
python -m uvicorn main:app --host 0.0.0.0 --port 5000 --workers 4
```

#### 验证
```bash
# 检查服务状态
curl http://localhost:5000/docs

# 测试API
curl -X POST http://localhost:5000/getModelTrainDetails \
  -H "Content-Type: application/json" \
  -d '{"model_id": 1, "user_id": 10}'
```

---

### Spring Boot后端

#### 环境准备
```bash
# 确保Java 8+已安装
java -version

# 确保Maven已安装
mvn -version
```

#### 启动服务
```bash
cd Backend(SpringBoot)

# 方式1：使用Maven
mvn spring-boot:run

# 方式2：使用Maven Wrapper
./mvnw spring-boot:run

# 方式3：运行jar包
mvn clean package
java -jar target/labelMark-0.0.1-SNAPSHOT.jar
```

#### 验证
```bash
# 检查服务状态
curl http://localhost:1290/wegismarkapi/mark/getModelList \
  -H "Content-Type: application/json" \
  -d '{"user_id": "10"}'
```

---

### 前端

#### 环境准备
```bash
# 确保Node.js已安装
node -v
npm -v

# 安装依赖
cd Frontend(React)/portal
npm install
```

#### 启动服务
```bash
# 开发模式
npm start

# 构建生产版本
npm run build

# 预览生产版本
npm run serve
```

#### 验证
- 访问 http://localhost:8000
- 登录系统
- 进入模型管理页面
- 点击"查看详情"按钮

---

## 🔍 功能验证

### 1. 模型管理页面

#### 访问路径
```
http://localhost:8000/modelManage
```

#### 验证步骤
1. ✅ 页面样式正常显示（紫色渐变主题）
2. ✅ 模型列表加载成功
3. ✅ 点击"启动模型训练"按钮，抽屉打开
4. ✅ 选择任务，配置参数
5. ✅ 点击"查看详情"按钮
6. ✅ 模型详情抽屉显示训练指标和参数

### 2. 个人任务列表页面

#### 访问路径
```
http://localhost:8000/personalTaskList
```

#### 验证步骤
1. ✅ 页面样式正常显示
2. ✅ 选项卡切换功能正常
3. ✅ 任务列表加载成功
4. ✅ 状态标签显示正确
5. ✅ 操作按钮功能正常

---

## 🐛 故障排查

### Python后端无法启动

**症状**：
```
ModuleNotFoundError: No module named 'fastapi'
```

**解决**：
```bash
pip install fastapi uvicorn
```

---

**症状**：
```
Address already in use
```

**解决**：
```bash
# 查找占用端口的进程
lsof -i :5000  # Mac/Linux
netstat -ano | findstr :5000  # Windows

# 杀死进程
kill -9 <PID>  # Mac/Linux
taskkill /PID <PID> /F  # Windows
```

---

### Spring Boot无法启动

**症状**：
```
Port 1290 is already in use
```

**解决**：
修改 `application.yml` 中的端口号，或杀死占用端口的进程。

---

**症状**：
```
Failed to connect to database
```

**解决**：
1. 检查数据库是否启动
2. 检查 `application.yml` 中的数据库配置
3. 测试数据库连接

---

### 前端无法启动

**症状**：
```
npm ERR! missing script: start
```

**解决**：
```bash
# 检查package.json中是否有start脚本
cat package.json | grep start

# 如果没有，使用
npm run dev
```

---

**症状**：
```
CORS error
```

**解决**：
1. 确认Python后端已启动
2. 检查Python后端的CORS配置
3. 清除浏览器缓存

---

### API调用失败

**症状**：
```
Failed to fetch
```

**解决**：
1. 检查Python后端是否启动（http://localhost:5000/docs）
2. 检查网络连接
3. 查看浏览器控制台错误信息
4. 查看Python后端日志

---

**症状**：
```
404 Not Found
```

**解决**：
1. 确认API路径正确
2. 检查Python后端是否有该接口
3. 查看FastAPI文档（http://localhost:5000/docs）

---

## 📊 系统状态检查

### 一键检查脚本

创建 `check_status.sh`：
```bash
#!/bin/bash

echo "=== 系统状态检查 ==="

# 检查Python后端
echo -n "Python FastAPI (5000): "
curl -s http://localhost:5000/docs > /dev/null && echo "✅ 运行中" || echo "❌ 未运行"

# 检查Spring Boot
echo -n "Spring Boot (1290): "
curl -s http://localhost:1290 > /dev/null && echo "✅ 运行中" || echo "❌ 未运行"

# 检查前端
echo -n "前端 (8000): "
curl -s http://localhost:8000 > /dev/null && echo "✅ 运行中" || echo "❌ 未运行"

# 检查数据库
echo -n "数据库: "
psql -h localhost -U postgres -c "SELECT 1" > /dev/null 2>&1 && echo "✅ 运行中" || echo "❌ 未运行"

echo "=== 检查完成 ==="
```

运行：
```bash
chmod +x check_status.sh
./check_status.sh
```

---

## 🔧 开发工具推荐

### API测试
- **Postman**: 测试API接口
- **curl**: 命令行测试
- **FastAPI Docs**: http://localhost:5000/docs

### 调试工具
- **Chrome DevTools**: 前端调试
- **Python Debugger**: 后端调试
- **IntelliJ IDEA**: Java开发

### 日志查看
```bash
# Python日志
tail -f Backend(FastAPI)/fastapi.log

# Spring Boot日志
tail -f Backend(SpringBoot)/logs/spring-boot.log

# 前端日志
# 浏览器控制台
```

---

## 📚 下一步

1. 阅读 [API接口测试指南.md](./API接口测试指南.md)
2. 查看 [模型管理和训练页面优化总结.md](./模型管理和训练页面优化总结.md)
3. 参考 [前端直连Python后端配置说明.md](./前端直连Python后端配置说明.md)
4. 完成 [部署检查清单.md](./部署检查清单.md)

---

## 💡 提示

- 开发时建议使用 `--reload` 模式，代码修改后自动重启
- 生产环境使用多worker模式提高性能
- 定期查看日志文件，及时发现问题
- 使用版本控制管理代码变更

---

**祝你使用愉快！** 🎉
